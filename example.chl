//Use FileIO.jar

def exp(base, power) {
    var i = 0;
    while (i < (power - 1)) {
        base = base * base;
    }
    return base;
}

var expTest = exp(2, 8);
if (expTest == 256) {
    println("Success!!!!");
}