bgn
ldi jid ;where jid is current index + 5
psh 0
jmp _func
pop 1
pch
end  ;endpoint of the program.

_func:
;your code here
psh 1 ;stack 1 is the return stack
pop 0 ;stack 0 is the call stack.
jmp cva
